import type { Knex } from 'knex';
import { DatabaseMetadata, ResourceMetadata } from '../metadata/index.js';
import { ConnectionOptions, DatabaseDialect } from './types/index.js';
export declare class BaseDatabaseParser {
    protected knex: Knex;
    protected dialect: DatabaseDialect;
    protected connectionOptions: ConnectionOptions;
    static dialects: DatabaseDialect[];
    constructor(dialect: DatabaseDialect, connection: ConnectionOptions);
    parse(): Promise<DatabaseMetadata>;
    getSchema(): Promise<string>;
    getTables(schemaName: string): Promise<string[]>;
    getResources(tables: string[], schemaName: string): Promise<ResourceMetadata[]>;
    getProperties(table: string, schemaName: string): Promise<any[]>;
}
